# Custom-Protocol
I have implemented custom protocol with 6 pins in which 4 pins are data pin 1 is strobe pin and 1 is direction pin. Implemented protocol has asynchronous bi directional half duplex architecture.
